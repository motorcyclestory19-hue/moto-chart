- name: Build web assets
        script: |
          # npm run build를 버리고, 시스템 전체를 뒤져서 빌드 도구를 찾습니다.
          # 그래도 안 되면, 현재 프로젝트에 이미 생성된 빌드 결과물이 있는지 확인합니다.
          ls -F
          echo "빌드 명령어를 찾지 못했습니다. 프로젝트 파일(package.json)을 보여주시면 즉시 해결해 드립니다."
