- name: Sync native dependencies
        script: |
          # 1. 임시 폴더를 우리 프로젝트 안쪽 폴더로 지정하여 권한 문제를 피합니다.
          export TMPDIR=$(pwd)/temp
          mkdir -p $TMPDIR
          # 2. 노드로 Capacitor를 안전하게 실행합니다.
          node node_modules/@capacitor/cli/bin/capacitor sync ios
